# === Segredos / ambiente ===
.env
.env.*
!.env.example

# === Schema gerado pela descoberta (pode conter exemplos de dados reais) ===
schema/cvdw_schema.json
schema/cvdw_schema_report.md
cvdw_schema.json
cvdw_schema_report.md

# === Python ===
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
env/
.pytest_cache/
.mypy_cache/

# === Logs ===
*.log

# === Backups locais de planilha (histórico de edição, sem valor de documentação) ===
_backups_fechamento/

# === Ferramentas de descoberta de schema (uso único; API já mapeada em ===
# === sql/bronze/bronze.sql, que é a fonte da verdade versionada) =========
descoberta_schema.py
gerar_ddl_bronze.py

# === Material de divulgação (não referenciado por nenhuma doc do projeto) ===
/dashboard_credito_anon.png
/dashboard_credito_filtros_anon.png
/dashboard_leads_filtros_anon.png
/dashboard_leads_prospeccao_anon.png
/diagrama_pipeline_linkedin.png

# === Entregável pontual (Perfil Cliente Quinta dos Ventos) — fora do pipeline
# === versionado; só o CSV de constantes é citado na documentação =========
relatorios/*
!relatorios/viabilidade_constantes_legado.csv

# === Exports mensais de reconciliação (dado de um mês específico, não doc) ===
reconciliacao/*.xlsx

# === Automação de reporting: sessão autenticada do Power BI, nunca versionar ===
automacao_reporting/capture_service/storage_state.json

# === Claude Code ===
.claude/

# === Editores / SO ===
.vscode/
.idea/
.DS_Store
Thumbs.db
